name: test CI/CD
on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: test deploy
      uses: appleboy/jenkins-action@master
      with:
        url: "http://192.168.1.10:31363/"
        user: "g9736ms"
        token: ${{ secrets.TOKEN }}
        job: "anthony test"
