def info():
    bucket_name = "버킷이름을 넣어준다"
    aws_name = "버킷에 있는 사용자 디렉터리 입력"
    aws_id = "엑세스키 입력"
    aws_key = "시크릿 키를 입력."
    #그리고 해당 값들을 리턴해준다.
    return bucket_name, aws_name, aws_id, aws_key
